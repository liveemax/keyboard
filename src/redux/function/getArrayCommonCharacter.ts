export const getArrayCommonCharacter=(speedType:number, typeScore:number, errorCount:number, typeCounter:number, disable:boolean)=>{
    return{speedType:[],typeScore:999,errorCount:errorCount,typeCounter:typeCounter,disable: disable}
}